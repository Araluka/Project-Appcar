import 'package:flutter/material.dart';

class VendorCarFormPage extends StatelessWidget {
  const VendorCarFormPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("จัดการรถของฉัน")),
      body: const Center(
        child: Text("TODO: CRUD Cars UI"),
      ),
    );
  }
}
